export const loginPath = {
  post: {
    tags: ['Login'],
    summary: 'Api for authenticate user',
    requestBody: {
      description: 'success',
      content: {
        'application/json': {
          schema: {
            $ref: '#/schemas/loginParams'
          }
        }
      }
    },
    responses: {
      200: {
        description: 'success',
        content: {
          'application/json': {
            schema: {
              $ref: '#/schemas/account'
            }
          }
        }
      }
    }
  }
}
